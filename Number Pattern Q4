let n = 4;
let size = 2 * n - 1;

for (let i = 0; i < size; i++) {

    let row = "";

    for (let j = 0; j < size; j++) {

        let min = Math.min(i, j, size - i - 1, size - j - 1);
        row += (n - min) + " ";

    }

    console.log(row.trim());
}
